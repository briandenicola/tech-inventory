# Copilot State

**Last session**: 2026-05-18 — Hicks domain reference entities T06-T10
**Current spec**: `specs/001-core-api/`
**Next action**: Continue Hicks T11-T17: add `AuditEvent` and `ImportBatch`, then wire `AppDbContext`, repository interfaces/classes, and audit stamping.

## Notes
- `./scripts/verify.ps1` is green on Windows: format, build, tests, vulnerability scan, frontend check, and frontend lint all pass
- Domain tests now cover `Household`, `Device`, `Brand`, `Category`, `Owner`, `Location`, `Network`, `Tag`, and `DeviceTag`
- `Category` stores both `ParentId` and `Depth` (1-3) so hierarchy depth is enforced in Domain before persistence
- Reference entities follow the `Brand` pattern with trimmed names, normalization helpers, and `IsActive` archive/reactivate flow
- `DeviceTag` is modeled as a composite identity with activation state instead of hard delete semantics
