# Copilot State

**Last session**: 2026-05-18 — Apone Round 5 domain recovery and T20-T28 handler tests
**Current spec**: `specs/001-core-api/`
**Next action**: Hicks can move on to T29+ import/export handlers or controller wiring, while Apone can shift to T45 integration coverage for the new controllers once HTTP endpoints land.

## Notes
- Domain coverage is restored above the constitutional floor with fresh merged Cobertura results at **100.00%**; Application coverage is now **85.89%** after converting all T20–T28 handler scaffolds into executable tests
- `tests\TechInventory.UnitTests\Application\` now contains real handler/validator coverage for Devices, Brands, Categories, Owners, Locations, Networks, Tags, device-tag add/remove, and ClaimDeviceOwnership; current backend suite summary is **266 passed / 0 skipped**
- `Error` now exposes `Conflict`, `NotFound`, and `Validation` factory helpers, and `ValidationRules` was normalized so the new handler validators compile cleanly under Release
- Category list/query semantics are now locked by tests: root nodes are paginated at the top level while descendants stay nested under `Children`; owner delete remains blocked when any device still references the owner
- Backend verification commands succeeded on Windows (`dotnet format --verify-no-changes`, `dotnet build -c Release`, `dotnet test -c Release`, fresh XPlat Cobertura runs); `scripts\verify.ps1` is otherwise healthy but cannot complete in this environment because `docker` is unavailable for the Playwright compose step
