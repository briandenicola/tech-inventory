# Copilot State

**Last session**: 2026-05-18 — Hicks Round 7 import/export close-out + OpenAPI export
**Current spec**: `specs/001-core-api/`
**Next action**: Hicks can package the backend close-out commits, while Apone can refresh T45/T46 import/export coverage and OpenAPI drift checks against the now-green repo-root verify pipeline.

## Notes
- `src\TechInventory.Application\Imports\` now owns the CSV preview/commit flow: CsvHelper parsing with trimmed/case-insensitive headers, inferred or explicit column mapping, shared `DeviceValidationRules`, lookup resolution/creation, immutable `ImportBatch` persistence, and per-device audit entries via multi-entry `IAuditContext`
- `src\TechInventory.Api\Controllers\ImportsController.cs` exposes `/api/v1/imports` preview/commit/list/detail endpoints with multipart upload handling, optional JSON mapping, `Imports:MaxFileSizeBytes` enforcement, and `ApiExceptionHandler` now maps payload-too-large failures to RFC 7807 `413`
- `src\TechInventory.Application\Exports\ExportDevicesQuery.cs`, `IDeviceExportService`, `src\TechInventory.Infrastructure\Persistence\Repositories\DeviceRepository.cs`, and `src\TechInventory.Api\Controllers\ExportsController.cs` now provide filtered JSON/CSV exports; CSV writes buffer asynchronously to avoid Kestrel sync-write violations and export ordering is SQLite-safe
- `src\TechInventory.Api\Program.cs` now seeds a default `Primary Household` (`USD`) when the database is empty and supports `export-openapi`; `Taskfile.yml` exposes `openapi:export`, and repo-root `openapi.yaml` was regenerated from the runtime document
- Verified on Windows from repo root: `dotnet format --verify-no-changes` ✅, `dotnet build -c Release` ✅, `dotnet test -c Release` ✅ (**370 total / 369 succeeded / 1 skipped / 0 failed**), `dotnet run --project src\TechInventory.Api\TechInventory.Api.csproj -c Release --no-build -- export-openapi` ✅; smoke on `http://localhost:8080` passed for imports preview/commit/list, exports JSON/CSV, and `/openapi/v1.json`
