# Tasks 005 — API Key Support

**Spec**: [`spec.md`](spec.md) | **Plan**: [`plan.md`](plan.md) | **ADR**: [`../../docs/adr/0003-api-key-authentication.md`](../../docs/adr/0003-api-key-authentication.md)
**Issue**: [#149](https://github.com/briandenicola/tech-inventory/issues/149)
**Last Updated**: 2026-09-03 — revised by Hicks (independent revision, reviewer lockout) after Apone's QC rejection

**Status of this branch: documentation/spec approval only — no task below is implemented.** All 25 tasks remain `⬜ Pending`.

---

## Wave 1 — API / Security / Contract / Tests / Ops

| Task | Owner | Depends | Authorized files | Description & acceptance |
|---|---|---|---|---|
| T-001 | Implementer | — | `Domain/Entities/ApiKey.cs`; `Domain/Enums/{ApiKeyScope,ApiKeyPrincipalType}.cs` (create) | `ApiKey` aggregate: Name, Selector, VerifierHash, Scope, `PrincipalType`, `PrincipalId`, HouseholdId, ExpiresAt, RevokedAt, RevokedBy + standard audit fields. `Revoke(actor)` idempotent; `IsActive(asOf)` = not revoked && not expired. Guard on Name(1–100)/Selector/VerifierHash. Zero framework deps. Builds with `dotnet build src/TechInventory.Domain`. |
| T-002 | Implementer | T-001 | `Infrastructure/Persistence/Configurations/ApiKeyConfiguration.cs`; `Infrastructure/Persistence/Repositories/ApiKeyRepository.cs`; `Infrastructure/Persistence/Migrations/*_AddApiKeys.cs`; `Infrastructure/Persistence/AppDbContext.cs` (DbSet); `Application/Abstractions/Repositories/IApiKeyRepository.cs` | Table `ApiKeys`; unique index `Selector`; index `(PrincipalType, PrincipalId)`; composite `(PrincipalType, PrincipalId, RevokedAt)` for quota. Repository methods async + `CancellationToken`, no raw SQL. Migration additive, applies to empty and existing DBs. |
| T-003 | Implementer | T-001, T-002 | `Application/Features/ApiKeys/{CreateApiKey,ListApiKeys,RevokeApiKey,CreateApiKeyValidator,RevokeApiKeyValidator}.cs`; `Application/Abstractions/Services/IApiKeyHasher.cs` | `CreateApiKeyCommand`: resolve caller `(PrincipalType, PrincipalId)` via `ICurrentUserService`/claims, resolve `HouseholdId` via `IHouseholdRepository.ListAsync` exactly-one (plan §3, same pattern as `CreateDeviceCommand`), quota check (≤5 active), 16-byte selector + 32-byte secret via `RandomNumberGenerator`, HMAC verifier via `IApiKeyHasher`, `IAuditable`. `ListApiKeysQuery`: paginated, never returns secret/hash. `RevokeApiKeyCommand`: caller = owning principal OR Admin, idempotent, `IAuditable`. FluentValidation: name 1–100, scope enum, expiresInDays 1–365. |
| T-004 | Implementer — **security-critical, T-023 review required** | T-002, T-003 | `Api/Authentication/{ApiKeyAuthHandler,ApiAuthenticationSchemes}.cs`; `Api/Program.cs`; `Infrastructure/Services/{HmacApiKeyHasher,ApiKeyOptions}.cs` | Custom `AuthenticationHandler<>` parsing `ApiKey <selector>.<secret>`. On selector **hit**: real `FixedTimeEquals` compare. On **miss**: dummy `FixedTimeEquals` compare against a fixed verifier (ADR) — identical call shape either branch, asserted by a unit test (call-count/code-path), **never by elapsed time**. Live-reloads principal by `PrincipalType` (`IOwnerRepository`/`ILocalUserRepository`); checks not-revoked/not-expired/`IsActive`/current `Role`. Builds `ClaimsPrincipal` (`oid`/`sub`=`PrincipalId`, `name`, `ClaimTypes.Role`, `apikey_selector`, `apikey_scope`). Registers `TechInventoryAuth.ApiKey`; extends `ForwardDefaultSelector` with the ambiguous-credential guard (checked first, `401`/`AmbiguousCredential`) then the `ApiKey ` prefix branch; Entra/Local branches untouched. Pepper: fail-fast at startup if absent or <32 decoded bytes, distinct from `Auth:Local:SigningKey`. Serilog log on every attempt (selector, principal id — never secret). All failures → uniform `401`/`InvalidApiKey`. |
| T-005 | Implementer | T-004 | `Api/Authentication/ApiKeyScopeRequirement.cs`; `Api/Program.cs` | `IAuthorizationRequirement` + handler: no-op when `apikey_scope` claim absent (bearer auth unaffected); else enforce ADR scope table (`inventory.read`=GET device+reference-data; `inventory.write`=+POST/PUT/DELETE device; both deny admin/audit/import/export/report/settings/local-auth). |
| T-006 | Implementer | T-003, T-004 | `Api/Controllers/ApiKeysController.cs`; `Api/Program.cs` (rate limiter) | Thin controller: `POST`/`GET`/`DELETE /api/v1/api-keys`, `GET /api/v1/api-keys/all` (Admin). Bearer-only — rejects `ApiKey` auth (N-10). Rate limiter bound to `RateLimiting:ApiKey:{PermitLimit,WindowSeconds}` configuration (prod default 60/60s) so integration tests can override to a small window — **no production-default flood in tests**. |
| T-007 | Implementer | T-004 | `Api/Program.cs` | Ambiguous-credential guard inside `ForwardDefaultSelector`, evaluated before any scheme forward: both `ApiKey`/`Bearer` present (multi-header or comma-joined) → `401`/`AmbiguousCredential`, no fallthrough. Single valid header unaffected for all three schemes (N-11). |
| T-008 | Implementer | T-006 | `openapi.yaml` | `ApiKeyAuth` security scheme (`type: apiKey`, header). Inventory endpoints list `BearerAuth`+`ApiKeyAuth`; key-management endpoints list `BearerAuth` only. Validates with an OpenAPI linter. |
| T-009 | Implementer | T-001, T-003 | `tests/TechInventory.UnitTests/Domain/ApiKeyTests.cs`; `tests/TechInventory.UnitTests/Application/ApiKeys/*.cs` | Entity invariants, `Revoke`/`IsActive`, Guard validations; HMAC hasher round-trip + dummy-path invocation assertion (call-count/mock, **not elapsed time**); validator bounds; quota logic. `dotnet test --filter ApiKey` green. |
| T-010 | Implementer — **security-critical, T-023 review required** | T-004..T-008 | `tests/TechInventory.IntegrationTests/ApiKeys/*.cs` | Full HTTP round-trip: create→use→revoke→401; all N-1..N-21 (spec §6) incl. N-19/N-20 bearer-routing regression and N-21 test-scoped rate limiting (`429` via a small configured window, never the 60/min default); quota (5 ok, 6th `409`); Admin cross-revocation; audit assertions; Serilog assertion selector present/secret absent. |
| T-011 | Implementer | T-008 | `tests/TechInventory.IntegrationTests/Contract/OpenApiDriftTests.cs` (update if needed) | Runtime-generated spec includes `ApiKeyAuth` scheme + new endpoints, matches `openapi.yaml`. |
| T-012 | Implementer | T-004 | `docs/operations.md` (update) | New "API Key Administration" section: pepper generation (`openssl rand -base64 48`, ≥32 decoded bytes), kept distinct from `Auth:Local:SigningKey`; rotation invalidates all keys; emergency revocation `curl` example; quota/expiry constants; Serilog properties to alert on. |
| T-013 | Implementer | T-009..T-012 | none (verification only) | `task verify` exits 0; no new `dotnet list package --vulnerable` findings; coverage floor held. |

## Wave 2 — Generated Client / Settings UI / Admin UI / Manual Checks

| Task | Owner | Depends | Authorized files | Description & acceptance |
|---|---|---|---|---|
| T-014 | Implementer (Web) | T-008, Wave 1 merged | `Web/src/lib/api/**` (regenerated) | TS client regen from updated `openapi.yaml`; `ApiKeysService` (`create`/`list`/`revoke`/`listAll`) compiles under `pnpm run check`. |
| T-015 | Implementer (Web) | T-014 | `Web/src/routes/(authenticated)/settings/api-keys/{+page.svelte,+page.ts}` | Personal key list/create/revoke; loading/empty/error/success states (§6.5.4); revoke confirmation; component <200 lines. |
| T-016 | Implementer (Web) | T-015 | `Web/src/lib/components/ApiKeySecretDisplay.svelte` | One-time secret display: read-only + copy-to-clipboard + "won't see this again" warning (i18n); cleared from state on dismiss; accessible labeling/focus. |
| T-017 | Implementer (Web) | T-015, T-016 | `Web/src/routes/(authenticated)/settings/api-keys/*.test.ts`; `Web/src/lib/components/ApiKeySecretDisplay.test.ts` | All 4 states covered; axe-core zero violations. |
| T-018 | Implementer (Web) | T-014 | `Web/src/routes/(authenticated)/admin/api-keys/{+page.svelte,+page.ts}` | All-keys list, owner-name filter, typed-name confirm revoke (§6.5.4), Admin-only guard. |
| T-019 | Implementer (Web) | T-018 | `Web/src/routes/(authenticated)/admin/api-keys/*.test.ts` | Loading/empty/filtered/error states; axe-core zero violations; revoke confirmation tested. |
| T-020 | Implementer (Web) | T-015, T-018 | `Web/src/lib/i18n/en.json` (update) | All API-key UI strings; no hard-coded text in `*.svelte` (§6.5.12); `pnpm run check` passes. |
| T-021 | Implementer (Web) | T-015, T-018 | `docs/testing/manual-pwa-validation.md` (update) | New M-items: Settings/Admin pages at 360px, offline cached Settings shell, copy-to-clipboard on iOS Safari/Android Chrome; owners assigned; not counted as automated coverage. |
| T-022 | Implementer | T-017, T-019, T-020, T-021 | none (verification only) | `task verify` exits 0 incl. frontend build/lint/`pnpm run check`; no new moderate+ `npm audit` findings. |

## Review / Verification

| Task | Owner | Depends | Authorized files | Description & acceptance |
|---|---|---|---|---|
| T-023 | Security reviewer (Bishop or equivalent) | T-004, T-010 | none (review only) | Inspects: fixed-time compare + dummy path on **both** hit/miss branches via design/code review, **not timing measurement**; pepper fail-fast + entropy + distinctness from local signing key; uniform `401`/`InvalidApiKey` across all failure modes; `401`/`AmbiguousCredential` checked before any scheme forward; redaction (no secret in logs/audit/responses); rate limiter is per-selector and test-scoped in CI, not bypassable by header tricks; live principal ceiling re-checked every request for both `PrincipalType`s. Sign-off required; no finding ≥ Medium. |
| T-024 | Lead reviewer (Ripley or equivalent) | T-013, T-022 | none (review only) | Clean Architecture compliance (no outward deps, no EF Core in Domain/controllers); thin controller; MediatR/FluentValidation used; audit via existing `AuditBehavior`; naming/placement; PR description cites spec sections. |
| T-025 | Brian (human approver) | T-023, T-024 | none | Merge implementation PR(s) referencing `#149`; issue closed by the implementation PR, not this spec revision; `task verify` green on the merge commit. |
