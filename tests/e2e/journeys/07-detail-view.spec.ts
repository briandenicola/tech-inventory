/**
 * Journey 7: Detail view (Viewer opens a device, sees all reference data resolved to human-readable labels)
 * 
 * PRD §7.5.4: Critical user journey #7
 * Verifies device detail view shows all fields with resolved references (e.g., Location name, not ID).
 */

import { test, expect } from '@playwright/test';

test.describe.skip('Journey 7: Detail view', () => {
  test.todo('Viewer can navigate to device detail from list');
  test.todo('Detail view shows all device fields');
  test.todo('Reference data is resolved to human-readable labels');
  test.todo('Owner, location, category display as names not IDs');
});
