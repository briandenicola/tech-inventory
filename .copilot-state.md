# Copilot State

**Last session**: 2026-05-18 — Hicks Round 6 controllers, ProblemDetails, and dev auth bypass
**Current spec**: `specs/001-core-api/`
**Next action**: Hicks can pick up T29/T30/T31/T39/T42 import-export work, while Apone can finish T45 controller integration coverage once her in-flight `DevAuthBypassTests` compile issue is resolved.

## Notes
- `src\TechInventory.Api\Controllers\` now exposes T32-T38 and T40 on classic attribute-routed `/api/v1/...` controllers, all `[Authorize]`, all dispatching through MediatR, with `/openapi/v1.json` enabled for Bruno/curl discovery
- `src\TechInventory.Api\Common\ControllerResultExtensions.cs` centralizes Result→HTTP success mapping; failures throw `ResultFailureException` and `ApiExceptionHandler` turns them into RFC 7807 ProblemDetails with validation `errors`, 404, and 409 shapes
- Development auth bypass is live behind `Auth:DevBypass` (`true` only in `appsettings.Development.json`, `false` elsewhere); synthetic requests authenticate as `dev-admin` with Admin role and fixed `oid/sub`, and audit stamping now reads claims through `HttpContextCurrentUserService`
- Smoke-tested on Windows at `http://localhost:8080`: OpenAPI 200, devices list 200 with empty `PagedResponse`, brand create 201 Created, validation failure 400 ProblemDetails, not-found 404 ProblemDetails, audit-events list 200 with created-brand audit row
- Repo-root `dotnet build -c Release` / `dotnet test -c Release` are currently blocked in this shared workspace by Apone's in-progress `tests\TechInventory.IntegrationTests\Controllers\DevAuthBypassTests.cs(94,40)` compile error; API-only build is green and `dotnet test -c Release` still executed 240 passing tests before the integration test project failed to compile
