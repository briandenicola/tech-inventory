/**
 * Journey 8: Import CSV (Admin uploads a CSV; preview shown; errors surfaced; commit succeeds)
 * 
 * PRD §7.5.4: Critical user journey #8
 * Verifies CSV import flow with preview, validation, and commit.
 */

import { test, expect } from '@playwright/test';

test.describe.skip('Journey 8: Import CSV', () => {
  test.todo('Admin can navigate to import page');
  test.todo('Admin can upload a valid CSV file');
  test.todo('Preview shows rows to be imported');
  test.todo('Validation errors are surfaced before commit');
  test.todo('Admin can commit import and see success message');
  test.todo('Imported devices appear in device list');
});
