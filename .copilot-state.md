# Copilot State

**Last session**: 2026-09-02 — T105 approval + agentic development foundation completed and pushed; awaiting QC/PR/merge
**Current branch**: `chore/agentic-development-foundation`
**Head**: `b94cb6a` (chore: enforce agentic development gates)
**Remote head**: `origin/chore/agentic-development-foundation` (in sync)
**Current phase**: Post-foundation QC and PR preparation

## T001–T105 Status

**Completed and approved at final reviewer gates:**
- **T001–T004**: Research, inventory, incident reconstruction, principles (DONE, documents only)
- **T103**: Coverage migration matrix and deletion map (DONE, analysis only, `coverage-migration.md`)
- **T102**: Valuable coverage migrated to lower-reliable layers (DONE, 649 Vitest + 296 integration tests APPROVED by Ripley 2026-09-02)
- **T101**: Playwright harness retired safely (DONE, `tests/e2e/**` deleted, 0 stale references, APPROVED by Ripley 2026-09-02)
- **T104**: Single authoritative verification interface, Playwright-free (DONE, `task verify` exit 0 end-to-end observed by Apone at `b3c092f`)
- **T105**: Required checks and tamper-tested controls (DONE, APPROVED by Bishop 2026-09-02, all six revision cycles closed, `check:security --repo` 0 matches across 940 files, full `task verify` exit 0 with complete changeset staged, index reset byte-identical; ADR 0002 and the constitution/PRD Playwright amendments completed)

**Commits delivered:**
- `b3c092f`: checkpoint agentic development foundation (T101–T104 implementation freeze)
- `764282e`: docs: record T104 approval
- `b94cb6a`: chore: enforce agentic development gates (T105 final, ADR 0002 and constitution/PRD amendments, branch-protection recommendation, CODEOWNERS/PR-template alignment)

## What Remains Before Merge

**Next immediate action:** Post-major-work QC audit (skill:post-major-work-qc-audit)
- Comprehensive engineering review: best practices, security, docs, architecture, test coverage, supply chain, UX, ops readiness
- GitHub Actions / CI execution not yet observed; confirm check context names match `.github/workflows/` job ids
- Branch protection recommendation recorded in `validation.md` §16.6; user to apply via GitHub UI if desired
- Main currently has no branch protection or rulesets (`404 Branch not protected`)
- Open PR: coordinate with operations for GitHub Actions identity and check context observation

**Blockers:**
- **F-17 follow-up** (highest-value): align `check-security.mjs --repo` to tracked + untracked non-ignored file enumeration
- **Migration-drift follow-up**: make the check clean/rebuild-safe so stale incremental build output cannot cause false results
- **Backlog prioritization**: 21 open issues (#127–#148 excluding #140 closed) ready to triage; recommended first work: #130, #129+#138, then the #133 data-integrity fix

## Notes
- `src\TechInventory.Web\src\lib\components\ResponsiveListCard.svelte` plus `ActionOverflowMenu.svelte` are the shared frontend seam for mobile list cards: admin pages should feed them heading text, filtered label/value fields, optional checkbox state, and overflow actions instead of open-coding bespoke mobile cards.
- `src\TechInventory.Web\src\lib\components\DeviceTable.svelte` intentionally keeps its own mobile renderer because grouped sections, status badges, and detail-open behavior are device-specific; reuse the local `getMobileFields(...)` pattern there rather than forcing devices through the admin wrapper.
- Full frontend validation for this pass is `pnpm run lint`, `pnpm run check`, `pnpm exec vitest run`, and `pnpm run build`; if build flakes on stale generated output, clear `src\TechInventory.Web\.svelte-kit` and rerun the build.
- `src\TechInventory.Application\BulkOperations\` is the shared seam for `BulkOperationResponse`, `BulkAuditEnvelope`, and the reference bulk-delete validator/interface helpers; the device bulk handlers now use that seam too.
- `src\TechInventory.Application\Networks\Commands\MergeNetworkCommand.cs` plus `IDeviceRepository.ReassignNetworkReferencesAsync(...)` / `src\TechInventory.Infrastructure\Persistence\Repositories\DeviceRepository.cs` add the missing network merge path so device `NetworkId` references can be reassigned before the source row is deactivated.
- `src\TechInventory.Api\Controllers\BrandsController.cs`, `CategoriesController.cs`, `LocationsController.cs`, and `NetworksController.cs` now expose `POST /bulk/delete`; `NetworksController.cs` also exposes `POST /merge`. Category bulk delete processes selected nodes deepest-first so selecting both a parent and child in one batch stays atomic.
- Repo-root `openapi.yaml` was refreshed from the runtime `/openapi/v1.json` document after booting `src\TechInventory.Api`.
- `src\TechInventory.Domain\Entities\HouseholdSetting.cs` plus `src\TechInventory.Infrastructure\Persistence\Repositories\HouseholdSettingRepository.cs` / `Configurations\HouseholdSettingConfiguration.cs` add the reusable per-household settings seam: unique `(HouseholdId, Key)` rows with JSON values and EF migration `20260520202952_AddHouseholdSettings`.
- `src\TechInventory.Application\Settings\` now owns the display-settings catalog, default arrays, allowlists, MediatR query/command handlers, and FluentValidation rules; persisted invalid/duplicate settings now surface as `409 Conflict` instead of silently leaking bad data.
- `src\TechInventory.Api\Controllers\SettingsController.cs` exposes `GET/PUT /api/v1/settings/display`; GET seeds default rows when missing, PUT is `Admin`-only, and successful writes append a `HouseholdSetting` audit event. Repo-root `openapi.yaml` was regenerated from the runtime document.
- `src\TechInventory.Web\src\routes\(authenticated)\devices\+page.svelte` now opens `src\TechInventory.Web\src\lib\components\DeviceDetailModal.svelte` from a `?device=` URL param and uses `DeviceListAddActions.svelte` so the create affordance stays route-linked (`/devices/new`), bottom-left safe-area-aware on mobile, and role-gated consistently across the header, FAB, and empty state.
- `src\TechInventory.Web\src\lib\components\DeviceActionsMenu.svelte` is the shared device overflow menu for both the modal and `devices\[id]\+page.svelte`; mobile gets a bottom action sheet, desktop gets a dropdown, and both reuse the same edit/claim/release/history/delete affordances.
- `src\TechInventory.Web\src\lib\components\DeviceDetailFields.svelte` now owns the horizontal key-value device table plus audit summary, so modal and direct detail views stay in sync without duplicating field markup.
- `src\TechInventory.Web\src\lib\components\RetireDeviceModal.svelte` plus `src\TechInventory.Web\src\lib\utils\deviceRetirement.ts` add the detail-screen retire flow; `src\TechInventory.Web\src\lib\stores\viewState.ts` keeps `/devices` filters/sort/search/group params in `sessionStorage` across detail/edit navigation while stripping the transient `device` modal param.
- The direct mobile device detail page hides breadcrumbs below `sm`, moves the content up with a small negative top offset, and keeps the title/actions in one row to reduce PWA wasted space.
- `src\TechInventory.Web\src\lib\components\MergeEntityModal.svelte` now supports both single-source and bulk-source merges for brands, categories, locations, and networks; `src\TechInventory.Web\src\lib\utils\referenceMerge.ts` handles device-count lookups plus repeated source→target merge orchestration, and the pages refresh `referenceDataStore` after success so dropdowns/forms drop merged-away entities immediately.
- `src\TechInventory.Web\src\lib\components\ReferenceDataBulkBar.svelte`, `BulkDeleteReferenceModal.svelte`, and `src\TechInventory.Web\src\lib\utils\referenceSelection.ts` are the shared frontend seam for F039: Set-based multi-select state, a sticky bulk action bar, and guarded bulk delete confirmation for brands/categories/locations/networks.
- `src\TechInventory.Web\src\lib\navigation\appNav.ts` is still the single source of truth for authenticated shell navigation. The top-level admin affordance now uses `navigation.adminAudit` and routes directly to `/admin/audit`, while `src\TechInventory.Web\src\routes\(authenticated)\admin\+page.ts` preserves `/admin` as a 307 redirect instead of rendering a duplicate hub.
- Follow-up cleanup: `src\TechInventory.Web\src\routes\(authenticated)\admin\{brands,categories,locations,networks}\+page.svelte` no longer renders a per-row/card `Merge` action. Merge is entered from the bulk bar only; page-local single-source helpers/toasts were pruned, while the shared modal + repeated merge orchestration remain in place for `Merge Selected`.
- The backend merge endpoints now live at `/api/v1/brands/merge`, `/api/v1/categories/merge`, and `/api/v1/locations/merge`; they require `Admin`, reassign device foreign keys, deactivate the source entity, and append paired `AuditEvent` rows for source/target context.
- `src\TechInventory.Web\src\routes\(authenticated)\reports\+page.svelte` now consumes `/api/v1/reports/summary` and `/api/v1/reports/warranties`, and also hosts `src\TechInventory.Web\src\lib\components\EraReportCard.svelte` / `EraReportDecade.svelte` plus `TimelineReport.svelte` / `TimelineBar.svelte` for the nostalgic era + timeline cards with shared category filtering patterns and explicit loading/error/empty states.
- `GET /api/v1/reports/eras` now returns active devices with non-null purchase dates grouped into newest-first decade buckets, optional `categoryId` filtering, per-decade `totalValue`, and up to three sample device names.
- `GET /api/v1/reports/insurance` now returns a dated CSV attachment with a generated-at comment line, optional `locationId` filter, active-device-only rows, and a `TOTAL` footer; repo-root `openapi.yaml` was regenerated from the runtime document so the merge/report schemas resolve again.
- `GET /api/v1/reports/timeline` now returns all devices with non-null purchase dates (including retired/disposed history), optional `categoryId` / `fromDate` / `toDate` filters, `groupBy` switching between category and owner labels, oldest-first ordering, and `asOfDate` / `appliedCategoryId` metadata; `TimelineReport.svelte` normalizes that payload into grouped lifespan bars with desktop year axes and mobile-friendly start/end labels.
- `src\TechInventory.Web\src\lib\utils\reports.ts` and the temporary wrappers in `src\TechInventory.Web\src\lib\api\client.ts` / `types.ts` can now be retired whenever Vasquez wants to switch back to a freshly generated client.
- Root auth bootstrap in `src\TechInventory.Web\src\routes\+layout.svelte` now runs `handleRedirectPromise()` and `tryAcquireApiTokenSilent()` before clearing the auth store, so cached Entra sessions auto-enter the app without exposing the login screen first.
- `src\TechInventory.Web\src\routes\auth\login\+page.svelte` now renders a short "Checking your household session…" splash during the silent check and only reveals the sign-in button after the Entra attempt misses; local-account fallback still stays available once loading completes.
- Silent SSO validation: `pnpm run check` ✅, changed-file ESLint + full `pnpm exec vitest run` + `pnpm run build` ✅, focused `pnpm exec vitest run src/lib/auth/index.test.ts` ✅ (**5 passed**). Repo `scripts\verify.ps1` now gets through backend/unit/integration/vulnerability/frontend linting and fails only when Playwright tries to call unavailable `docker` in this environment.
- `src\TechInventory.Web\src\lib\components\admin\ResponsiveAdminList.svelte` now centralizes the shared mobile-card / desktop-table pattern for brands, locations, networks, owners, and tags; `admin\categories\+page.svelte` keeps its tree view on `md+` and swaps to parent-aware cards on smaller screens.
- Responsive admin-table validation: `pnpm run check` ✅, `pnpm run lint` ✅, full `pnpm exec vitest run` ✅ (**338 passed / 1 skipped**, including new `ResponsiveAdminList` axe coverage), `pnpm run build` ✅.
- `src\TechInventory.Web\src\lib\components\DeviceFilters.svelte` now follows the mobile-sheet pattern needed for iPhone PWAs: `h-dvh` container, sticky header/footer, safe-area padding, body scroll lock, and a `min-h-0 flex-1 overflow-y-auto` scroll region so the close button never scrolls out of reach. `src\TechInventory.Web\src\lib\components\DeviceFilters.test.ts` covers dialog semantics, Escape close, sticky chrome, and axe cleanliness; the relevant Playwright follow-up lives in `tests\e2e\journeys\06-browse-filter.spec.ts` plus `13-a11y-smoke.spec.ts`.
- F045 PWA shell: `displayMode.svelte.ts` wraps `isStandalonePwa()` reactively; `AppBottomNav` renders home/add/reports/settings pill + theme-toggle + identity bubble; `AppMenuPopover` anchored popover reuses desktop menu structure; `DevicePwaRow` renders grouped two-line device list; `DeviceTable` split into three renderers (desktop/mobile/pwa). Implicit PWA category grouping via `effectiveGroupBy` logic, separate from URL-written `urlFilters.groupBy`. 500-row cap scoped to PWA caller only. All D-175 through D-187 decisions merged into `.squad/decisions.md`.
