# Copilot State

**Last session**: 2026-05-18 — Hudson SQLite integration harness + hermetic E2E contract
**Current spec**: `specs/001-core-api/`
**Next action**: Hicks can land T11-T17 with `AppDbContext` wired to `ConnectionStrings:Default`; once the first migration exists, Apone can extend the new `IntegrationTestFactory<TMarker>` pattern across the API suite.

## Notes
- `task test:integration` now runs `tests/TechInventory.IntegrationTests` against `IntegrationTestFactory<TMarker>` and a fresh SQLite file per test class
- `IntegrationTestFactory<TMarker>` no-ops cleanly when no `DbContext` is registered, but will call `Database.Migrate()` automatically once Hicks lands `AppDbContext` + migrations
- `task test:e2e` now owns compose lifecycle (`up --build` → wait for `/health/ready` → Playwright on `http://localhost:3000` → `down -v`) via `scripts/run-e2e.ps1` / `scripts/run-e2e.sh`
- `scripts/verify.ps1` and `scripts/verify.sh` now split backend unit and integration runs before the hermetic Playwright step
- Verified on Windows: `dotnet build tests\TechInventory.IntegrationTests\TechInventory.IntegrationTests.csproj -c Release` and `task test:integration`
