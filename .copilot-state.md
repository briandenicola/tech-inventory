# Copilot State

**Last session**: 2026-05-18 — Phase 1 Round 7 close-out
**Current spec**: `specs/001-core-api/` (COMPLETE ✅)
**Phase**: Phase 1 complete; awaiting Phase 2 kickoff from Brian. Dev auth bypass (`Auth:DevBypass=true`) active for local API access without Entra OIDC wiring.
**Next milestone**: Phase 2 — SvelteKit UI + real Entra OIDC Bearer token wiring

## Notes
- `tests\TechInventory.IntegrationTests\Controllers\ImportsControllerTests.cs` and `ExportControllerTests.cs` now cover T45 import/export happy + error paths on the real SQLite `WebApplicationFactory` harness, including config-driven 413 import limits, lookup auto-creation during commit, CSV/JSON exports, filtered exports, and large-dataset reads
- `tests\TechInventory.IntegrationTests\Contract\` now carries reusable OpenAPI canonicalization/schema helpers plus drift assertions between runtime `/openapi/v1.json` and committed `openapi.yaml`; covered endpoint schema checks are executable, and only the export 200-body schema assertion remains skipped because the runtime/committed spec omit that response schema
- Repo-root backend verification is green: `dotnet format --verify-no-changes`, `dotnet build -c Release`, `dotnet test -c Release --no-build`, and `dotnet list package --vulnerable --include-transitive`; current backend summary is **369 passed / 1 skipped**, and merged coverage is **Domain 100.00% / Application 91.58% / Infrastructure 94.33% / Api 91.63%**
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\verify.ps1` advances through frontend `pnpm install --frozen-lockfile`, `pnpm run check`, and `pnpm run lint`, but the Playwright step is blocked in this Windows environment because `docker` is not installed
