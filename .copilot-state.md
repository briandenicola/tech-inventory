# Copilot State

**Last session**: 2026-05-18 — Hicks T16-T19 repositories, interceptors, and behaviors
**Current spec**: `specs/001-core-api/`
**Next action**: Hicks can move into T20+ command/query handlers while Apone adds consumer-side tests around the new repository and behavior seams.

## Notes
- `src/TechInventory.Infrastructure/Persistence/Repositories/` now contains `BrandRepository`, `CategoryRepository`, `DeviceRepository`, `HouseholdRepository`, `OwnerRepository`, `LocationRepository`, `NetworkRepository`, `TagRepository`, `AuditEventRepository`, and `ImportBatchRepository` on top of the shared `Repository<TEntity, TKey>` base
- `AuditSaveChangesInterceptor` is registered through `AppDbContext.OnConfiguring(...)` and stamps UTC `CreatedAt` / `ModifiedAt` plus `system` actors via `ICurrentUserService` / `SystemCurrentUserService`
- Application now exposes `IAuditable`, scoped `IAuditContext`, `ValidationBehavior<TRequest, TResponse>`, and `AuditBehavior<TRequest, TResponse>`; validation failures return `Error.Code = "Validation"` with `Error.ValidationErrors`, and audit payloads come from handler-provided BEFORE state plus request JSON fallback for AFTER
- Verified on Windows: `dotnet format --verify-no-changes`, `dotnet build -c Release`, and `dotnet test -c Release` all pass; attempted `scripts/verify.ps1`, but it stalled during the build phase after repeated waits
